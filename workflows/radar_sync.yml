name: M11 OSINT Radar Sync
on:
  schedule:
    - cron: '0 */6 * * *' # Αυτόματο σκανάρισμα κάθε 6 ώρες
  workflow_dispatch:      # Η "Χειροκίνητη Σκανδάλη" για άμεσο τρέξιμο

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: pip install requests beautifulsoup4

      - name: Run Fleet Commander OSINT
        run: python scrapers/fleet_commander.py

      - name: Commit & Push Results
        run: |
          git config --global user.name 'NauticalOS-Bot'
          git config --global user.email 'bot@nauticalos.com'
          # Δημιουργία φακέλου data αν δεν υπάρχει
          mkdir -p data
          git add data/radar_feed.json
          git commit -m "M11: Radar Intelligence Sync [Auto]" || exit 0
          git push
